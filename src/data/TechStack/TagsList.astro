---
interface Props {
  tags: string[];
}
const { tags } = Astro.props;
---

<div class="flex flex-wrap gap-y-1 p-1 tracking-wide">
  {
    tags &&
      tags.map((tag) => (
        <span class="badge-secondary badge badge-xs">{tag}</span>
      ))
  }
</div>
