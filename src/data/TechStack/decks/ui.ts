// import type { TechDeck } from "../types";

import type { TechDeck } from "../types";

const ui = {
  "Material UI": { iconBgColor: "responsive", icon: "logos:material-ui" },
} satisfies TechDeck;

export default ui;
