// import type { TechDeck } from "../types";

import type { TechDeck } from "../types";

const ui = {
  "Material UI": { icon: { bgColor: "black", name: "logos:material-ui" } },
} satisfies TechDeck;

export default ui;
