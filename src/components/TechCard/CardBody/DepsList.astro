---
import { Sprite } from "astro-iconify";
interface Props {
  deps: string[];
}
const { deps } = Astro.props;
---

<span class="flex h-4 gap-1 opacity-60">
  {deps && deps.map((dep) => <Sprite name={dep} class="aspect-square" />)}
</span>
