---
import type { HTMLAttributes } from "astro/types";
import { twMerge } from "tailwind-merge";

export interface Props extends HTMLAttributes<"button"> {
  noDefaultPadding?: boolean;
  fill?: "solid" | "outline" | "none";
  // variant?: "accent" | "neutral";
  hoverBorder?: boolean;
}

const {
  noDefaultPadding = false,
  hoverBorder = false,
  class: extraClass,
  ...attrs
} = Astro.props;
---

<button
  class={twMerge(
    !noDefaultPadding && "py-3 px-4",
    "font-semibold rounded-lg bg-cc-blue text-cc-white dark:hover:bg-cc-white dark:hover:text-cc-blue hover:text-cc-white hover:bg-cc-space justify-center items-center select-none",
    hoverBorder &&
      "border border-transparent hover:border-cc-white dark:hover:border-cc-space",
    // "font-semibold rounded-lg bg-cc-blue text-cc-white justify-center items-center select-none hover:opacity-70",
    extraClass
  )}
  {...attrs}
>
  <slot />
</button>
