---
interface SectionProps extends astroHTML.JSX.HTMLAttributes {
  fullWidth: boolean;
  forehead: boolean;
  secondaryBg: boolean;
}
const {
  fullWidth,
  forehead,
  secondaryBg,
  class: className,
} = Astro.props as SectionProps;
---

<section
  class:list={[
    {
      "px-4 xl:px-0 xl:max-w-screen-xl mx-auto": !fullWidth,
      "pt-12": forehead,
      "text-white bg-black dark:bg-invert-bgSecondary": secondaryBg,
    },
    className,
  ]}
>
  <slot />
</section>
